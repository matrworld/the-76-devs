{
    "name": "prisma-shared",
    "version": "1.0.0",
    "main": "./node_modules/prisma-shared/",
    "scripts": {
        "db:push": "npx prisma db push",
        "db:generate": "npx prisma generate",
        "build": "pnpm run db:generate && tsc -p tsconfig.json"
    },
    "devDependencies": {
        "prisma": "^5.13.0",
        "prisma-zod-generator": "^0.8.13",
        "typescript": "^5.4.5"
    },
    "dependencies": {
        "@prisma/client": "5.13.0"
    }
}